


<h1>List of users</h1>

<section>
    <% @users.each do |user| %>        
          <a href= <%= "users/#{user.id}"%>>
          <%= image_tag(user.photo)%>
            <h2> <%= user.name %></h2>
              Number of posts:
              <%= user.posts_counter || 0 %>
            </div>
            </div>
          </a> 
    <% end %>
</section>